{
  "name": "@browser-bridge/telemetry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Per-tool-call instrumentation: sizes and timings to a rotating NDJSON log, never argument values",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}

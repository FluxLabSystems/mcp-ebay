// Moved to packages/compact (@browser-bridge/compact) so the gateway's ebay_api_search tool can apply the
// identical search.include/fields/limit/offset semantics and warning codes to vendor rows (COUNTDOWN-API-PLAN §6.1).
export * from '@browser-bridge/compact';
